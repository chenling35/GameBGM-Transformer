# ============================================================
# GameBGM-Transformer 依赖清单
# 说明：带 [未直接import] 注释的包是疑似多余依赖，保留但注释掉
# 用 pip install -r requirements.txt 安装所有启用的包
# ============================================================

# ---------- 深度学习核心 ----------
torch>=2.0.0
# transformers 在 src/ 和 backend/ 中均未直接 import；
# EMO-Disentanger 的推理脚本通过 subprocess 调用，不由本 requirements 管理。
# 若直接使用 GPT-2/Transformer 模型，取消注释：
# transformers>=4.30.0

# ---------- MIDI 处理 ----------
miditoolkit>=1.0.1          # EMO-Disentanger representations/ 核心依赖
mido>=1.3.0                 # MIDI 文件读写
midi2audio>=0.1.1           # FluidSynth 封装，后端 MIDI→WAV 转换
pretty_midi>=0.2.10         # midi_emotion/preprocess.py 和 label_vgmusic.py 使用

# ---------- 数据处理 ----------
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0
pyyaml>=6.0                 # 读取 EMO-Disentanger YAML 配置文件

# ---------- 机器学习工具 ----------
scikit-learn>=1.2.0         # label_vgmusic.py: GradientBoosting 情感分类器
# scipy 在 src/ 和 backend/ 中均未发现直接 import，疑似早期规划遗留
# scipy>=1.10.0

# ---------- 可视化（训练监控） ----------
# matplotlib 在当前代码中未使用，疑似早期规划遗留
# matplotlib>=3.7.0
# tensorboard 在当前代码中未使用，日志目录 runs/ 已在 .gitignore 中忽略
# tensorboard>=2.13.0

# ---------- 后端服务 ----------
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6     # FastAPI 文件上传支持（multipart/form-data）
pydantic>=2.0.0             # FastAPI BaseModel 依赖，backend/main.py 直接 import
